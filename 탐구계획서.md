# 고급 지구과학 탐구 계획서

## 1. 탐구 주제

**지능정보기술을 활용한 해수면 온도·위도 기반 태풍 강도 예측: 단순 물리 시뮬레이션과 인공지능 잔차 보정 모델 비교**

이 주제는 제목만 읽어도 `지능정보기술 활용`, `지구과학 대기·해양 분야`, `탐구 대상(태풍 강도 변화)`가 드러나도록 설정하였다.

## 2. 주제 선정 이유 및 탐구 동기

태풍은 해양과 대기의 상호작용으로 발달하는 대표적인 지구과학 현상이다. 특히 해수면 온도는 태풍에 에너지를 공급하는 중요한 요인이고, 태풍이 저위도에서 고위도로 이동할수록 주변 해양·대기 환경이 달라지면서 강도 변화가 나타난다. 이러한 내용은 고급 지구과학에서 다루는 대기-해양 상호작용, 에너지 이동, 기압계와 폭풍의 발달 원리와 직접 연결된다.

그러나 실제 태풍 강도 변화는 해수면 온도와 위도만으로 완전히 설명되지 않는다. 같은 해수면 온도 조건에서도 급격한 강화나 약화가 발생할 수 있으며, 여기에는 연직 바람 전단, 습도, 내부 구조 변화 등 비선형적인 요인이 함께 작용한다. 이처럼 물리적으로는 설명 가능한 큰 경향과, 단순 물리식만으로는 설명하기 어려운 세부 변화가 동시에 존재한다는 점이 흥미로웠다.

이에 본 탐구에서는 먼저 해수면 온도와 위도를 이용해 태풍 강도 변화를 설명하는 단순 물리 시뮬레이션 모델을 만들고, 이후 인공지능이 물리 모델의 예측 오차만을 학습해 보정하도록 설계하고자 한다. 이를 통해 물리 기반 해석 가능성과 인공지능의 패턴 학습 능력을 결합했을 때 태풍 강도 예측이 얼마나 개선되는지 분석하고자 한다.

## 3. 탐구 목적

본 탐구의 목적은 다음과 같다.

1. 해수면 온도와 위도 변화가 태풍 강도 변화에 미치는 기본적인 영향을 단순 물리 모델로 구현한다.
2. 단순 물리 모델이 태풍 강도 변화의 전체 경향을 어느 정도 설명할 수 있는지 확인한다.
3. 인공지능 잔차 보정 모델이 물리 모델의 예측 오차를 줄일 수 있는지 정량적으로 분석한다.
4. `MLP`, `LSTM`, `Transformer` 중 어떤 모델이 태풍 강도 변화의 시계열 보정에 더 적합한지 비교한다.

## 4. 탐구 문제, 대상 및 범위

### 4-1. 탐구 문제

1. 해수면 온도와 위도 변화는 태풍 강도 변화의 기본 경향을 어느 정도 설명하는가?
2. 단순 물리 시뮬레이션만으로는 설명하기 어려운 오차는 어떤 구간에서 크게 나타나는가?
3. 인공지능 보정 모델은 물리 모델의 예측 오차를 유의미하게 감소시킬 수 있는가?
4. 시계열 정보를 반영하는 모델(`LSTM`, `Transformer`)이 단순 구조 모델(`MLP`)보다 더 효과적인가?

### 4-2. 탐구 대상

본 탐구의 대상은 실제 태풍 관측자료를 단순화해 만든 **합성 태풍 시계열 데이터**이다. 각 태풍 사례에는 다음 변수가 포함된다.

- 위도(`latitude`)
- 해수면 온도(`SST`)
- 태풍 강도(`intensity`)
- 숨겨진 환경 요인인 전단(`shear`)
- 숨겨진 환경 요인인 습도(`humidity`)

### 4-3. 탐구 범위

탐구 범위는 다음과 같이 제한한다.

1. 태풍의 발생 여부나 이동 경로 전체가 아니라 **시간에 따른 강도 변화 예측**에 초점을 둔다.
2. 실제 자료 대신 실험 통제가 쉬운 합성 데이터를 사용하되, 태풍의 물리적 경향과 비선형성을 함께 반영한다.
3. 물리 모델은 해수면 온도, 위도, 포화 효과, 기본 감쇠만 반영한 단순 규칙 기반 모델로 제한한다.
4. 인공지능 모델은 강도 자체를 직접 예측하지 않고, 물리 모델의 오차인 **잔차(residual)** 만 예측하도록 제한한다.
5. 성능 평가는 `MAE`, `RMSE`를 중심으로 수행한다.

## 5. 선행연구 분석

### 5-1. Emanuel (1987)

**출처**  
Emanuel, K. A. (1987). *The dependence of hurricane intensity on climate*. *Nature, 326*, 483-485. [https://doi.org/10.1038/326483a0](https://doi.org/10.1038/326483a0)

**분석 내용**  
이 연구는 열기관(Carnot cycle) 관점에서 허리케인 최대 강도를 설명하며, 더 따뜻한 해양 환경이 더 큰 잠재 강도를 만들 수 있음을 보였다. 즉 태풍 강도는 단순히 바람 자체의 현상이 아니라 해양으로부터 공급되는 에너지와 깊게 연결된다는 점을 이론적으로 제시하였다.

**본 탐구 활용 방안**  
본 탐구에서 해수면 온도를 물리 모델의 핵심 입력 변수로 두는 근거로 활용한다. 또한 태풍 강도 예측에서 해양 조건이 중요한 이유를 이론적 배경으로 제시하는 데 사용한다.

### 5-2. DeMaria & Kaplan (1994)

**출처**  
DeMaria, M., & Kaplan, J. (1994). *Sea Surface Temperature and the Maximum Intensity of Atlantic Tropical Cyclones*. *Journal of Climate, 7*(9), 1324-1334. [https://doi.org/10.1175/1520-0442(1994)007<1324:SSTATM>2.0.CO;2](https://doi.org/10.1175/1520-0442(1994)007%3C1324:SSTATM%3E2.0.CO;2)

**분석 내용**  
이 연구는 북대서양 태풍 자료를 바탕으로 해수면 온도와 태풍 최대 강도 사이의 경험적 관계를 제시하였다. 해수면 온도가 높을수록 더 큰 잠재 강도에 도달할 수 있으나, 실제 태풍이 항상 그 한계에 도달하는 것은 아니라는 점도 함께 보여 주었다. 즉 환경 조건은 강도의 상한을 결정하지만 실제 강도 변화에는 추가 요인이 개입함을 시사한다.

**본 탐구 활용 방안**  
본 탐구에서 단순 물리 모델은 해수면 온도와 위도에 기반해 강도 변화를 설명하되, 실제 기준 데이터에는 전단·습도·기억 효과 등 추가 요인을 넣어 오차가 남도록 설계할 예정이다. 이는 선행연구가 말한 “기본 환경은 중요하지만 실제 강도는 더 복합적”이라는 점을 탐구 구조에 반영한 것이다.

### 5-3. Su et al. (2020)

**출처**  
Su, H., Wu, L., Jiang, J. H., Pai, R., Liu, A., Zhai, A. J., Tavallali, P., & DeMaria, M. (2020). *Applying Satellite Observations of Tropical Cyclone Internal Structures to Rapid Intensification Forecast With Machine Learning*. *Geophysical Research Letters, 47*(17), e2020GL089102. [https://doi.org/10.1029/2020GL089102](https://doi.org/10.1029/2020GL089102)

**분석 내용**  
이 연구는 태풍 강도 변화가 대규모 환경뿐 아니라 내부 구조와도 관련됨을 전제로, 위성 관측 기반 내부 구조 정보와 기계학습을 결합해 급강화 예측 성능을 높였다. 즉 태풍 강도 변화에는 단순 평균 환경값만으로 포착하기 어려운 비선형 패턴이 존재하며, 머신러닝이 이를 학습할 수 있음을 보여 준다.

**본 탐구 활용 방안**  
본 탐구에서도 단순 물리 모델이 설명하지 못하는 비선형 변화가 잔차 형태로 남는다고 가정한다. 그리고 인공지능이 바로 그 잔차를 학습하도록 설계함으로써, 선행연구의 핵심 논리인 “머신러닝은 숨은 패턴을 보완적으로 학습할 수 있다”는 점을 축소 모형 수준에서 검증한다.

### 5-4. Lee et al. (2024)

**출처**  
Lee, J., Im, J., & Shin, Y. (2024). *Enhancing tropical cyclone intensity forecasting with explainable deep learning integrating satellite observations and numerical model outputs*. *iScience, 27*(6), 109905. [https://doi.org/10.1016/j.isci.2024.109905](https://doi.org/10.1016/j.isci.2024.109905)

**분석 내용**  
이 연구는 위성 관측과 수치예보모델 출력을 결합한 하이브리드 딥러닝 모델이 단독 예보보다 태풍 강도 예측 성능을 개선할 수 있음을 보였다. 즉 물리 기반 수치모델과 데이터 기반 딥러닝을 대립적으로 보는 것이 아니라, 서로 다른 장점을 결합하는 방식이 효과적이라는 점을 보여 준다.

**본 탐구 활용 방안**  
본 탐구는 이 선행연구의 방향을 고등학생 수준에서 구현 가능한 형태로 단순화한다. 수치예보모델 대신 단순 물리 시뮬레이션을 사용하고, 딥러닝은 그 예측값을 직접 대체하지 않고 잔차만 보정하게 함으로써 해석 가능성과 실험 가능성을 동시에 확보한다.

## 6. 탐구 내용 및 차별성

본 탐구의 핵심 내용은 **“태풍 강도 변화의 기본 경향은 물리 모델이 설명하고, 물리 모델이 놓친 부분은 인공지능이 보정한다”** 는 구조를 검증하는 것이다.

기존 선행연구들은 주로 실제 위성자료, 수치예보모델, 복잡한 운영 예보 체계를 활용해 태풍 강도를 예측한다. 반면 본 탐구는 다음과 같은 점에서 차별성이 있다.

1. 실제 운영 예보 수준의 복잡한 모델 대신, **해수면 온도와 위도 중심의 단순 물리 시뮬레이션**을 직접 설계한다.
2. 실제 자료를 그대로 사용하는 대신, **숨겨진 비선형 요소가 포함된 합성 태풍 데이터**를 만들어 오차 발생 원인을 통제된 조건에서 분석한다.
3. 인공지능이 강도 자체를 처음부터 예측하지 않고, **물리 모델의 잔차만 학습**하도록 해 결과 해석을 더 명확하게 한다.
4. 동일한 데이터와 조건에서 `MLP`, `LSTM`, `Transformer`를 비교해 **어떤 형태의 인공지능이 보정 문제에 더 적합한지** 확인한다.

즉 본 탐구의 차별성은 “태풍 강도 예측 그 자체”보다도, **지구과학적 물리 해석과 인공지능 보정이 어떤 방식으로 결합될 수 있는가**를 교육적으로 드러낸다는 데 있다.

## 7. 탐구 방법 및 절차 설계

### 7-1. 탐구 환경

- 프로그래밍 언어: `Python`
- 사용 도구: `NumPy`, `Pandas`, `PyTorch`, `scikit-learn`, `Matplotlib`
- 실행 환경: 개인용 컴퓨터의 `CPU` 또는 사용 가능 시 `GPU`
- 산출물: 원시 데이터 파일, 예측 결과 파일, 성능 지표 표, 비교 그래프, 학습 곡선

### 7-2. 데이터 설계

총 `120`개의 태풍 사례를 생성하고, 각 사례는 `28~42`개의 시간 단계로 구성할 계획이다. 재현 가능성을 위해 난수 시드는 `42`로 고정한다. 주요 변수의 생성 범위와 규칙은 다음과 같다.

| 변수 | 생성 범위 또는 기준 | 생성 규칙 |
| --- | --- | --- |
| 시간 단계 수 | `28~42` | 각 태풍마다 정수 난수로 선택 |
| 초기 위도 | `8.0~14.0°N` | 저위도 해역에서 시작하도록 설정 |
| 위도 전체 범위 | `6.5~32.0°N` | 시간에 따라 `0.18~0.42°/step` 정도 북상하고, 작은 진동과 잡음을 더한 뒤 범위를 넘지 않게 자름 |
| 해수면 온도(SST) | `24.6~31.2°C` | 따뜻한 해역(`28.7~30.8°C`)에서 시작하되 위도가 높아질수록 감소하도록 설계 |
| 초기 강도 | `24.0~42.0 m/s` | 태풍 발달 초기 수준에서 시작 |
| 강도 전체 범위 | `15.0~85.0 m/s` | 너무 약하거나 비현실적으로 강한 값은 제한 |
| 전단(shear) | `2.0~18.0 m/s` | 위도가 높을수록 대체로 증가하도록 설정 |
| 습도(humidity) | `0.40~0.96` | SST가 높을수록 증가하고, 위도가 높을수록 감소하도록 설정 |

위도와 해수면 온도는 단순 난수가 아니라 다음 경향을 따르도록 만든다.

\[
\text{latitude}(t)=\text{lat}_{start}+d\cdot t+m\sin(\omega t+\phi)+\epsilon
\]

여기서 `lat_start`는 `8.0~14.0`, 북상 속도 `d`는 `0.18~0.42`, 진동 크기 `m`은 `0.15~0.55` 범위에서 정하고, 잡음 `\epsilon`을 더한 뒤 최종값을 `6.5~32.0` 사이로 제한한다.

\[
\text{SST}(t)=W-0.10\cdot \text{latitude}(t)+0.42\sin(0.24t+\phi_1)+0.18\cos(0.11t+\phi_2)+\epsilon
\]

여기서 따뜻한 해역 기준값 `W`는 `28.7~30.8` 범위에서 정하고, 최종 SST는 `24.6~31.2°C`로 제한한다.

이렇게 하면 위도와 SST가 태풍 강도 변화의 기본 경향을 만들고, 전단·습도·기억 효과는 추가 복잡성을 부여해 물리 모델에 남는 오차를 인공지능이 학습할 수 있게 된다.

### 7-3. 단순 물리 모델 설계

단순 물리 모델은 다음 원리를 반영한다.

1. 해수면 온도가 높을수록 강도 증가량이 커진다.
2. 일정 온도 이하에서는 냉각 효과로 약화가 커진다.
3. 위도가 높아질수록 북상에 따른 감쇠 효과가 커진다.
4. 강도가 지나치게 커지면 포화 효과가 발생한다.
5. 시간 경과에 따른 기본 감쇠가 존재한다.

물리 모델의 다음 시점 강도 변화량은 다음과 같이 구체적으로 정의한다.

\[
\Delta I_{physics}=1.55(\text{SST}-27.0)-0.95\max(26.4-\text{SST},0)-0.55\max(\text{latitude}-18.0,0)-0.045\max(I_t-60.0,0)-\left(0.40+0.05\max(\text{latitude}-24.0,0)\right)
\]

\[
\hat{I}_{physics}(t+1)=\min\left(85,\max\left(15,\ I_t+\Delta I_{physics}\right)\right)
\]

각 항의 의미는 다음과 같다.

1. `1.55(SST-27.0)`은 해수면 온도가 `27°C`보다 높을수록 강화되는 효과를 나타낸다.
2. `0.95 max(26.4-SST, 0)`는 차가운 해역에서의 약화 효과를 나타낸다.
3. `0.55 max(latitude-18.0, 0)`는 고위도 북상에 따른 감쇠 효과를 나타낸다.
4. `0.045 max(I_t-60.0, 0)`는 강도가 매우 커질수록 더 이상 빠르게 세지기 어려운 포화 효과를 나타낸다.
5. `0.40+0.05 max(latitude-24.0, 0)`는 시간 경과와 고위도 이동에 따른 기본 감쇠 항이다.

이 모델은 해양-대기 상호작용의 기본 방향은 설명하지만, 전단·습도·기억 효과·급강화 같은 복잡성은 직접 반영하지 못하도록 일부러 단순화한다. 따라서 이 물리 모델은 “기본 경향 설명용 기준선” 역할을 한다.

### 7-4. 인공지능 보정 모델 설계

인공지능 모델은 다음 시점 강도 자체가 아니라 **물리 모델의 오차(잔차)** 를 예측한다.

\[
\text{Residual} = y(t+1) - \hat{y}_{physics}(t+1)
\]

\[
\hat{y}_{final}(t+1) = \hat{y}_{physics}(t+1) + \hat{r}_{AI}(t+1)
\]

입력 변수는 최근 `6`개 시점의 다음 정보로 구성한다.

- 태풍 강도
- 해수면 온도
- 위도
- 물리 모델의 다음 시점 예측값
- 물리 모델의 예측 변화량

비교할 모델은 다음 세 가지이다.

1. `MLP`
2. `LSTM`
3. `Transformer`

### 7-5. 평가 방법

데이터는 `학습 70%`, `검증 15%`, `테스트 15%`로 분할한다. 모델 학습은 최대 `40 epoch`까지 수행하되, 검증 손실이 개선되지 않으면 조기 종료한다.

평가 지표는 다음과 같다.

- `MAE(Mean Absolute Error)`
- `RMSE(Root Mean Squared Error)`

또한 단순한 수치 비교에 그치지 않고,

1. 모델별 오차 막대그래프 비교
2. 일부 태풍 사례의 실제 강도-예측 강도 시계열 비교
3. 학습 손실 및 검증 손실 곡선 분석

을 함께 수행하여 어떤 방식으로 오차가 줄어드는지 해석할 계획이다.

또한 같은 시드(`42`)와 같은 데이터 분할 규칙을 사용해 반복 실행했을 때 결과가 크게 달라지지 않는지도 함께 확인하여 재현 가능성을 확보할 계획이다.

### 7-6. 세부 절차

1. 태풍 강도 변화와 해수면 온도, 위도, 전단, 습도 관련 선행연구를 정리한다.
2. 합성 태풍 시계열 데이터를 생성한다.
3. 단순 물리 모델로 다음 시점 강도를 예측한다.
4. 실제값과 물리 예측값의 차이를 잔차로 계산한다.
5. 잔차를 목표값으로 하여 `MLP`, `LSTM`, `Transformer`를 각각 학습한다.
6. 테스트 데이터에서 물리 모델 단독 결과와 AI 보정 결과를 비교한다.
7. 성능 차이를 그래프와 표로 정리한다.
8. 어떤 환경에서 물리 모델이 잘 맞고, 어떤 환경에서 인공지능 보정이 큰 효과를 내는지 해석한다.

## 8. 예상 결과 및 기대 효과

선행연구에 따르면 태풍 강도 변화는 해수면 온도 같은 대규모 환경 요인에 의해 큰 틀이 결정되지만, 내부 구조와 비선형 상호작용 때문에 단순 물리식만으로는 급격한 변화까지 정확히 맞히기 어렵다. 따라서 본 탐구에서도 단순 물리 모델은 태풍 강도의 **전반적인 증가·감소 경향**은 설명하겠지만, 급강화나 세부 변동에서는 비교적 큰 오차를 보일 것으로 예상된다.

반면 인공지능 보정 모델은 물리 모델이 놓친 패턴을 추가로 학습하므로, `MAE`와 `RMSE`가 물리 모델 단독일 때보다 유의미하게 감소할 것으로 예상된다. 특히 최근 시계열 흐름을 반영하는 `LSTM`이나 `Transformer`가 단순 `MLP`보다 더 우수한 보정 성능을 보일 가능성이 있다.

본 탐구의 기대 효과는 다음과 같다.

1. 태풍 강도 변화가 해양과 대기의 상호작용에 의해 결정된다는 지구과학적 원리를 구체적으로 이해할 수 있다.
2. 물리 모델과 인공지능 모델이 경쟁 관계가 아니라 **상호 보완 관계**가 될 수 있음을 확인할 수 있다.
3. 복잡한 실제 예보 체계를 단순화해 봄으로써, 고등학생 수준에서도 지구과학 탐구에 인공지능을 의미 있게 활용할 수 있음을 보여 줄 수 있다.
4. 향후 실제 태풍 관측자료를 활용한 확장 탐구의 기초 자료로 활용할 수 있다.

## 9. 탐구 일정 및 탐구일지 작성 계획

| 단계 | 내용 | 기록할 탐구일지 내용 |
| --- | --- | --- |
| 1단계 | 선행연구 조사 및 주제 구체화 | 읽은 자료, 핵심 개념, 연구에 반영할 요소 |
| 2단계 | 데이터 생성 규칙 설계 | 변수 정의, 범위 설정 이유, 수정 과정 |
| 3단계 | 물리 모델 구현 | 사용한 식, 가정, 한계 |
| 4단계 | AI 보정 모델 학습 | 입력 변수, 학습 과정, 오류 수정 내용 |
| 5단계 | 결과 분석 및 그래프 작성 | 모델별 차이, 대표 사례 해석 |
| 6단계 | 결론 및 한계 정리 | 탐구 의의, 개선 방향 |

탐구일지에는 단순한 작업 기록만이 아니라, **왜 그런 설계를 선택했는지**, **어떤 오류가 있었고 어떻게 수정했는지**, **그래프를 어떻게 해석했는지**까지 함께 기록할 계획이다.

## 10. 표절 예방 계획

1. 선행연구의 문장을 그대로 옮기지 않고, 핵심 의미를 이해한 뒤 자신의 문장으로 재구성한다.
2. 탐구계획서와 최종 보고서에 사용한 모든 논문·자료는 반드시 출처를 표기한다.
3. 그래프와 표는 직접 생성한 결과만 사용하고, 외부 그림을 사용할 경우 출처를 명시한다.

## 11. 참고문헌

1. Emanuel, K. A. (1987). *The dependence of hurricane intensity on climate*. *Nature, 326*, 483-485. [https://doi.org/10.1038/326483a0](https://doi.org/10.1038/326483a0)
2. DeMaria, M., & Kaplan, J. (1994). *Sea Surface Temperature and the Maximum Intensity of Atlantic Tropical Cyclones*. *Journal of Climate, 7*(9), 1324-1334. [https://doi.org/10.1175/1520-0442(1994)007<1324:SSTATM>2.0.CO;2](https://doi.org/10.1175/1520-0442(1994)007%3C1324:SSTATM%3E2.0.CO;2)
3. Su, H., Wu, L., Jiang, J. H., Pai, R., Liu, A., Zhai, A. J., Tavallali, P., & DeMaria, M. (2020). *Applying Satellite Observations of Tropical Cyclone Internal Structures to Rapid Intensification Forecast With Machine Learning*. *Geophysical Research Letters, 47*(17), e2020GL089102. [https://doi.org/10.1029/2020GL089102](https://doi.org/10.1029/2020GL089102)
4. Lee, J., Im, J., & Shin, Y. (2024). *Enhancing tropical cyclone intensity forecasting with explainable deep learning integrating satellite observations and numerical model outputs*. *iScience, 27*(6), 109905. [https://doi.org/10.1016/j.isci.2024.109905](https://doi.org/10.1016/j.isci.2024.109905)
