# 실제 태풍 관측자료 기반 물리 시뮬레이션과 인공지능 보정 모델 분석 v2

## 1. 탐구 동기

태풍 강도 변화는 해수면 온도, 위도, 시간에 따른 발달 단계, 주변 대기 환경 등 여러 요인의 영향을 받는다. 단순한 물리 규칙만으로도 따뜻한 바다 위에서 태풍이 강해지고, 고위도로 이동하면서 약화되는 큰 흐름은 설명할 수 있다. 그러나 실제 태풍은 급격한 강화, 일시적 정체, 빠른 약화처럼 단순 규칙만으로 설명하기 어려운 변화도 보인다.

기존 보고서는 합성 데이터를 중심으로 물리 모델과 인공지능 보정 모델을 비교하였다. v2에서는 실험의 현실성을 높이기 위해 실제 태풍 best track 자료인 NOAA IBTrACS와 실제 해수면 온도 자료인 NOAA OISST를 사용하였다. 즉, 이번 보고서는 생성한 가상 태풍이 아니라 실제 관측 기반 태풍 시계열을 대상으로 같은 구조의 분석을 다시 수행한 결과이다.

## 2. 탐구 목적

본 탐구의 목적은 다음과 같다.

1. 실제 태풍 관측자료를 사용하여 단순 물리 모델의 예측 성능을 확인한다.
2. NOAA OISST 해수면 온도 자료를 태풍 중심 위치에 결합하여 입력 변수로 사용한다.
3. 인공지능 모델이 물리 모델의 예측 오차를 보정할 수 있는지 정량적으로 분석한다.
4. `MLP`, `LSTM`, `Transformer` 중 어떤 보정 모델이 실데이터에서 가장 낮은 오차를 보이는지 비교한다.

## 3. 탐구 문제

1. 실제 IBTrACS 태풍 자료에서도 해수면 온도와 위도 기반 단순 물리 모델이 강도 변화의 기본 경향을 설명할 수 있는가?
2. 단순 물리 모델이 남기는 오차를 인공지능 보정 모델이 줄일 수 있는가?
3. 시계열 구조를 반영하는 모델이 단순 신경망보다 더 효과적인가?
4. 실데이터 기반 실험에서 해석할 때 주의해야 할 한계는 무엇인가?

## 4. 가설

실제 태풍 자료에서는 합성 데이터보다 변동성이 크기 때문에 단순 물리 모델의 오차가 더 크게 나타날 것이다. 그러나 물리 모델이 예측한 값과 최근 시점의 강도, 해수면 온도, 위도를 함께 입력하면 인공지능 모델이 남은 오차 패턴을 일부 학습할 수 있을 것이다. 따라서 AI 보정 모델은 물리 모델 단독 예측보다 낮은 `MAE`와 `RMSE`를 보일 것으로 예상하였다.

## 5. 사용 데이터

이번 v2 실험은 실데이터 경로로 새로 실행하였다.

| 항목 | 내용 |
| --- | --- |
| 데이터 소스 | NOAA IBTrACS + NOAA OISST |
| 해역 | 서태평양, `WP` basin |
| 기간 | 2020년 |
| 태풍 수 | 4개 |
| 총 시계열 행 수 | 108개 |
| 시간 간격 | 6시간 |
| 강도 자료 | `TOKYO_WIND`, 결측 시 `WMO_WIND` 보완 |
| 해수면 온도 | NOAA OISST v2.1 AVHRR 일별 격자 |
| 산출물 폴더 | `outputs/report_v2` |

사용된 태풍 ID와 시계열 길이는 다음과 같다.

| 태풍 ID | 시계열 길이 |
| --- | ---: |
| `2020239N13134` | 23 |
| `2020244N25146` | 28 |
| `2020277N21141` | 27 |
| `2020299N11144` | 30 |

실험 과정에서는 IBTrACS의 위도, 경도, 시간, 강도 값을 읽고, 각 태풍 중심 위치에서 가장 가까운 OISST 격자점의 해수면 온도를 추출하였다. 이렇게 만든 시계열을 학습, 검증, 테스트 구간으로 나누어 모델을 비교하였다.

## 6. 탐구 방법

### 6-1. 전체 구조

전체 실험은 다음 순서로 진행하였다.

1. IBTrACS에서 2020년 서태평양 태풍 자료를 불러온다.
2. 각 태풍 중심 위치에 대응하는 OISST 해수면 온도를 결합한다.
3. 단순 물리 모델이 다음 시점 강도를 예측한다.
4. 실제 강도와 물리 예측값의 차이를 잔차로 계산한다.
5. 인공지능 모델이 이 잔차를 학습한다.
6. 최종 예측값을 `물리 예측값 + AI 보정값`으로 계산한다.
7. 물리 모델 단독 결과와 AI 보정 결과를 비교한다.

이번 구조의 핵심은 인공지능이 태풍 강도를 처음부터 직접 예측하지 않는다는 점이다. 물리 모델이 먼저 예측을 수행하고, 인공지능은 그 예측에서 남은 오차를 보정한다.

### 6-2. 단순 물리 모델

단순 물리 모델은 해수면 온도, 위도, 현재 강도를 이용하여 다음 시점 강도를 예측한다. 주요 가정은 다음과 같다.

- 해수면 온도가 높을수록 강화 가능성이 커진다.
- 일정 기준보다 낮은 해수면 온도에서는 약화 효과가 커진다.
- 위도가 높아질수록 약화 요인이 증가한다.
- 강도가 이미 매우 높으면 추가 강화가 제한된다.
- 모든 물리 과정을 완전히 계산하지 않고, 해석 가능한 단순 규칙으로 제한한다.

이 모델은 실제 태풍의 복잡한 환경을 모두 반영하지 않는다. 대신 해석 가능하고 비교 기준으로 사용하기 쉬운 물리 기반 baseline 역할을 한다.

### 6-3. 인공지능 보정 모델

AI 보정 모델의 입력 변수는 다음 다섯 가지이다.

- 최근 시점의 태풍 강도
- 최근 시점의 해수면 온도
- 최근 시점의 위도
- 물리 모델의 다음 시점 예측값
- 물리 모델의 예측 변화량

시계열 입력 길이는 `6`으로 설정하였다. 비교한 모델은 다음과 같다.

| 모델 | 특징 |
| --- | --- |
| `MLP` | 고정 길이 입력을 펼쳐서 사용하는 기본 신경망 |
| `LSTM` | 시간 순서를 직접 반영하는 순환 신경망 |
| `Transformer` | 시점 간 관계를 attention 구조로 학습하는 모델 |

AI 모델의 출력은 최종 강도 자체가 아니라 잔차이다.

\[
\text{Residual} = y - \hat{y}_{physics}
\]

\[
\hat{y}_{final} = \hat{y}_{physics} + \hat{r}_{AI}
\]

## 7. 실험 결과

### 7-1. 정량적 성능 비교

테스트셋 결과는 다음과 같다.

| 모델 | MAE | RMSE | MAE 개선율 | RMSE 개선율 |
| --- | ---: | ---: | ---: | ---: |
| Physics | 3.1341 | 3.5783 | 0.00% | 0.00% |
| MLP | 2.7675 | 3.2481 | 11.70% | 9.23% |
| LSTM | 2.0528 | 2.4005 | 34.50% | 32.92% |
| Transformer | 1.8622 | 2.4828 | 40.58% | 30.62% |

세 AI 보정 모델은 모두 물리 모델 단독 예측보다 낮은 `MAE`를 보였다. 가장 낮은 `MAE`는 `Transformer`의 `1.8622`였고, 가장 낮은 `RMSE`는 `LSTM`의 `2.4005`였다. 이는 AI 보정이 단순 물리 모델의 오차를 줄이는 데 효과가 있었음을 보여준다.

![그림 1. 실데이터 테스트셋에서 물리 모델과 AI 보정 모델의 오차 비교](outputs/report_v2/metrics_comparison.png)

그림 1에서 물리 모델의 오차가 가장 크고, AI 보정 모델을 결합했을 때 오차가 줄어드는 경향을 확인할 수 있다. 특히 `LSTM`과 `Transformer`는 `MLP`보다 더 큰 개선을 보였는데, 이는 최근 시간 흐름을 반영하는 구조가 태풍 강도 변화 예측에 도움이 되었기 때문으로 해석할 수 있다.

### 7-2. 검증셋 결과

검증셋 결과는 다음과 같다.

| 모델 | MAE | RMSE | MAE 개선율 | RMSE 개선율 |
| --- | ---: | ---: | ---: | ---: |
| Physics | 2.6246 | 3.5063 | 0.00% | 0.00% |
| MLP | 2.5459 | 3.4852 | 3.00% | 0.60% |
| LSTM | 2.4331 | 3.3616 | 7.30% | 4.13% |
| Transformer | 2.3924 | 3.6319 | 8.85% | -3.58% |

검증셋에서는 테스트셋보다 개선 폭이 작았다. 또한 `Transformer`는 검증셋에서 `MAE`는 개선했지만 `RMSE`는 악화되었다. 이는 표본 수가 작을 때 큰 오차 몇 개가 `RMSE`에 민감하게 반영될 수 있음을 보여준다.

### 7-3. 시계열 예측 양상

![그림 2. 실제 태풍 강도와 각 모델의 예측 궤적 비교](outputs/report_v2/sample_trajectories.png)

그림 2는 테스트 구간에서 실제 강도와 모델 예측값을 비교한 것이다. 물리 모델은 전체적인 변화 방향을 따라가지만 일부 구간에서 실제 강도와 차이가 발생한다. AI 보정 모델은 이 차이를 줄이는 방향으로 예측을 조정한다.

특히 실제 태풍 자료에서는 강도 변화가 합성 데이터보다 불규칙하다. 따라서 AI 보정 모델이 모든 시점을 완벽하게 맞추지는 못하지만, 물리 모델이 크게 벗어나는 구간에서 예측값을 실제값 쪽으로 이동시키는 효과가 나타났다.

### 7-4. 학습 과정

![그림 3. AI 보정 모델의 학습 손실과 검증 손실 변화](outputs/report_v2/training_curves.png)

학습 기록을 보면 `MLP`와 `LSTM`은 14 epoch에서, `Transformer`는 13 epoch에서 조기 종료되었다. 이는 검증 손실이 더 이상 충분히 개선되지 않았기 때문이다. 작은 실데이터셋에서는 과적합 위험이 크므로, 조기 종료는 필요한 장치이다.

## 8. 결과 해석

첫째, 실제 태풍 자료에서도 단순 물리 모델은 비교 기준으로 의미가 있었다. 해수면 온도와 위도만으로 모든 변화를 설명할 수는 없지만, 태풍 강도 변화의 기본 방향을 잡는 baseline으로 사용할 수 있었다.

둘째, AI 보정 모델은 물리 모델의 잔차를 줄였다. 테스트셋에서 `MLP`, `LSTM`, `Transformer` 모두 물리 모델보다 낮은 `MAE`를 기록하였다. 특히 `Transformer`는 `MAE` 기준으로 약 `40.58%` 개선되었다.

셋째, 시계열 정보를 반영하는 모델의 효과가 나타났다. `LSTM`과 `Transformer`는 `MLP`보다 낮은 테스트 오차를 보였다. 태풍 강도는 한 시점의 조건만이 아니라 직전 몇 시점의 변화 흐름에 영향을 받기 때문에, 시간 구조를 반영하는 모델이 유리할 수 있다.

넷째, 결과 해석에는 주의가 필요하다. 이번 실험은 2020년 서태평양 태풍 4개만 사용했기 때문에 표본 수가 작다. 따라서 특정 모델이 항상 가장 우수하다고 일반화하기보다는, 실데이터에서도 물리 모델과 AI 보정을 결합하는 접근이 가능하다는 예비 검증으로 보는 것이 적절하다.

## 9. 한계 및 개선점

본 탐구의 한계는 다음과 같다.

1. 실데이터를 사용했지만 태풍 수가 4개로 작다.
2. 해수면 온도와 위도 외의 중요한 환경 변수, 예를 들어 연직 바람 전단, 습도, 해양 열함량, 이동 속도는 직접 입력하지 않았다.
3. IBTrACS의 강도 자료는 기관별 관측 방식 차이와 결측 처리 문제를 포함할 수 있다.
4. OISST는 일별 격자 자료이므로 6시간 간격 태풍 변화의 세부 해양 반응을 완전히 반영하지 못한다.
5. 테스트셋이 작아 `MAE`, `RMSE`가 개별 사례에 민감하게 변할 수 있다.

향후 개선 방향은 다음과 같다.

1. 2018년부터 2023년까지 기간을 넓혀 더 많은 태풍을 포함한다.
2. 연직 바람 전단, 상대습도, 이동 속도 등 대기 환경 변수를 추가한다.
3. 물리 모델을 더 정교하게 만든 뒤에도 AI 보정 효과가 유지되는지 확인한다.
4. 태풍별 분리 검증을 강화하여 특정 태풍에 대한 과적합 가능성을 줄인다.
5. 여러 seed로 반복 실험하여 모델 순위의 안정성을 확인한다.

## 10. 결론

v2 실험에서는 합성 데이터가 아니라 실제 NOAA IBTrACS 태풍 자료와 NOAA OISST 해수면 온도 자료를 사용하여 물리 모델과 AI 보정 모델을 비교하였다. 테스트셋에서 물리 모델 단독 예측의 `MAE`는 `3.1341`, `RMSE`는 `3.5783`이었다. AI 보정 모델을 적용한 결과 `MLP`, `LSTM`, `Transformer` 모두 `MAE`를 낮췄으며, 가장 좋은 `MAE`는 `Transformer`의 `1.8622`였다.

따라서 이번 실험은 실제 태풍 자료에서도 단순 물리 모델과 인공지능 보정 모델을 결합하면 예측 오차를 줄일 수 있음을 보여준다. 다만 데이터 규모가 작기 때문에 최종 결론은 “실데이터 기반 가능성 확인”에 가깝다. 더 많은 태풍과 추가 환경 변수를 포함하면 모델의 일반화 성능을 더 엄밀하게 평가할 수 있다.

## 11. 재현 방법 및 산출물

이번 v2 산출물은 다음 명령으로 새로 생성하였다.

```bash
python main.py --ibtracs-start-year 2020 --ibtracs-end-year 2020 --max-real-storms 4 --epochs 25 --output-dir outputs/report_v2
```

주요 산출물은 다음 위치에 저장되어 있다.

| 파일 | 설명 |
| --- | --- |
| `outputs/report_v2/storm_tracks.csv` | 실험에 사용된 실제 태풍 시계열 |
| `outputs/report_v2/data_source_summary.json` | 실데이터 출처와 필터링 조건 |
| `outputs/report_v2/metrics.csv` | 검증셋과 테스트셋 성능 지표 |
| `outputs/report_v2/predictions.csv` | 물리 모델과 AI 보정 모델의 예측값 |
| `outputs/report_v2/training_history.csv` | 모델별 학습 손실 기록 |
| `outputs/report_v2/summary.json` | 실험 설정과 테스트 결과 요약 |
| `outputs/report_v2/metrics_comparison.png` | 모델별 오차 비교 그래프 |
| `outputs/report_v2/sample_trajectories.png` | 실제 강도와 예측 강도 시계열 비교 |
| `outputs/report_v2/training_curves.png` | 학습 및 검증 손실 곡선 |

