# 해수면 온도와 위도 변화에 따른 태풍 강도 변화의 단순 물리 시뮬레이션과 인공지능 기반 보정 모델 분석

## 1. 탐구 동기

태풍의 강도 변화는 해수면 온도, 위도, 시간 경과, 내부 구조 변화 등 여러 요인의 영향을 받는다. 물리 모델은 이러한 변화를 원리적으로 설명하는 데 유용하지만, 실제와 유사한 복잡한 비선형 변화까지 모두 반영하기에는 한계가 있다. 반면 인공지능은 복잡한 패턴을 학습하는 데 강점이 있으나, 처음부터 현상을 직접 예측하도록 설계하면 해석 가능성이 낮아질 수 있다.

이에 본 탐구에서는 먼저 해수면 온도와 위도에 따라 태풍 강도가 변하는 단순 물리 시뮬레이션 모델을 구현하고, 이후 인공지능 모델이 물리 모델의 예측 오차만을 추가로 학습하여 보정하도록 설계하였다. 이를 통해 물리 기반 접근과 인공지능 기반 접근을 결합했을 때 예측 오차를 줄일 수 있는지 분석하고자 하였다.

## 2. 탐구 목적

본 탐구의 목적은 다음과 같다.

1. 해수면 온도와 위도 변화가 태풍 강도 변화에 미치는 영향을 단순 물리 모델로 구현한다.
2. 단순 물리 모델이 태풍 강도 변화의 전체적인 경향을 어느 정도 설명할 수 있는지 확인한다.
3. 인공지능 보정 모델이 물리 모델의 예측 오차를 얼마나 줄일 수 있는지 정량적으로 분석한다.
4. 여러 인공지능 모델 중 어떤 모델이 물리 모델 보정에 더 효과적인지 비교한다.

## 3. 탐구 문제

1. 해수면 온도와 위도 변화는 태풍 강도 변화에 어떤 영향을 미치는가?
2. 단순 물리 모델은 태풍 강도 변화를 어느 정도까지 설명할 수 있는가?
3. 인공지능 보정 모델은 단순 물리 모델의 예측 오차를 줄일 수 있는가?
4. 어떤 인공지능 모델이 물리 모델 보정에 가장 효과적인가?

## 4. 가설

단순 물리 모델은 태풍 강도 변화의 전체적인 경향은 설명할 수 있으나, 급격한 강화나 세부적인 비선형 변화까지 완전히 반영하지는 못할 것이다. 따라서 인공지능 보정 모델이 물리 모델의 오차 패턴을 추가로 학습하면 평균 오차가 감소하고, 더 정확한 태풍 강도 예측이 가능해질 것이라고 예상하였다.

## 5. 이론적 배경

태풍은 일반적으로 따뜻한 해수면 위에서 에너지를 공급받으며 발달한다. 따라서 해수면 온도가 높을수록 태풍이 강화될 가능성이 커진다. 반면 위도가 높아지면 상대적으로 차가운 해역으로 이동하거나 주변 환경이 불리해져 강도가 약화되기 쉽다. 이러한 점을 고려하면 해수면 온도는 강도를 증가시키는 요인, 위도 증가는 강도를 감소시키는 요인으로 단순화할 수 있다.

그러나 실제 태풍 강도 변화는 이보다 더 복잡하다. 일정 구간에서 급격한 강화가 나타날 수 있고, 강도가 충분히 높아졌을 때는 포화 효과가 발생할 수 있으며, 시간에 따른 내부 구조 변화도 영향을 준다. 따라서 단순 물리 모델만으로는 전체 추세는 설명하더라도 세부 오차가 남을 수 있다.

본 탐구에서는 이러한 구조를 반영하기 위해, 물리 모델은 비교적 단순하게 유지하고 실제 기준 데이터는 추가적인 비선형성과 시계열 효과를 포함하는 방식으로 설계하였다. 이후 인공지능 모델은 물리 모델이 남긴 오차만을 학습하도록 하였다.

## 6. 탐구 설계 및 방법

### 6-1. 전체 설계

본 탐구는 다음 순서로 진행하였다.

1. 합성 태풍 시계열 데이터를 생성한다.
2. 단순 물리 모델로 다음 시점의 태풍 강도를 예측한다.
3. 실제값과 물리 예측값의 차이인 잔차를 계산한다.
4. 인공지능 모델이 이 잔차를 학습하도록 한다.
5. 최종 예측값을 `물리 예측값 + AI 보정값`으로 계산한다.
6. 물리 모델과 AI 보정 모델들의 오차를 비교한다.

즉 인공지능이 강도 자체를 처음부터 직접 예측하는 것이 아니라, 물리 모델의 한계를 보완하는 구조로 설계하였다.

### 6-2. 데이터 생성

실험에는 실제 관측 자료 대신 합성 시계열 데이터를 사용하였다. 총 `120`개의 태풍 사례를 생성하였고, 각 사례는 `28~42`개의 시간 단계로 구성되도록 하였다. 각 태풍에 대해 다음 변수들을 만들었다.

- 위도(`latitude`)
- 해수면 온도(`SST`)
- 태풍 강도(`intensity`)
- 숨겨진 환경 변수인 전단(`shear`)
- 숨겨진 환경 변수인 습도(`humidity`)

기준 데이터 생성 과정에서는 해수면 온도가 높을수록 강도가 증가하고, 위도가 높아질수록 약화되는 기본 경향을 반영하였다. 동시에 급강화 구간, 포화 효과, 시간에 따른 기억 효과, 습도 및 전단의 간접 효과를 함께 넣어 실제와 비슷한 복잡성을 부여하였다.

### 6-3. 단순 물리 모델

단순 물리 모델은 다음과 같은 특징을 갖도록 설계하였다.

- 해수면 온도가 높을수록 태풍이 강화됨
- 임계 온도 이하에서는 약화가 커짐
- 위도가 높아질수록 감쇠 효과가 커짐
- 강도가 너무 강해지면 포화 효과가 작용함
- 시간 경과에 따른 기본 감쇠가 존재함

이 모델은 전체적인 강도 변화 방향은 설명할 수 있지만, 숨겨진 환경 요인과 비선형적 급강화 패턴은 충분히 반영하지 못하도록 일부러 단순화하였다.

### 6-4. 인공지능 보정 모델

인공지능 모델은 물리 모델이 예측한 값의 오차를 학습하였다. 사용한 입력 변수는 다음과 같다.

- 현재 및 최근 시점의 태풍 강도
- 최근 시점의 해수면 온도
- 최근 시점의 위도
- 물리 모델의 다음 시점 예측값
- 물리 모델의 예측 변화량

시계열 입력 길이는 `6`으로 설정하였다. 비교한 인공지능 모델은 다음 세 가지이다.

- `MLP`
- `LSTM`
- `Transformer`

이때 인공지능의 출력은 최종 강도 자체가 아니라 보정량, 즉 잔차값이다.

\[
\text{Residual} = y - \hat{y}_{physics}
\]

\[
\hat{y}_{final} = \hat{y}_{physics} + \hat{r}_{AI}
\]

### 6-5. 평가 방법

데이터는 학습용, 검증용, 테스트용으로 분할하였다. 성능 평가는 다음 두 지표를 사용하였다.

- `MAE(Mean Absolute Error)`
- `RMSE(Root Mean Squared Error)`

이 값이 작을수록 예측이 실제값에 가깝다는 뜻이다.

## 7. 탐구 결과

### 7-1. 정량적 성능 비교

테스트셋에서 얻은 결과는 다음과 같다.

| 모델 | MAE | RMSE | MAE 개선율 |
| --- | ---: | ---: | ---: |
| Physics | 0.8503 | 1.0874 | 0.00% |
| MLP | 0.4137 | 0.5242 | 51.35% |
| LSTM | 0.3919 | 0.4962 | 53.91% |
| Transformer | 0.4378 | 0.5746 | 48.51% |

위 표를 보면 세 인공지능 보정 모델 모두 물리 모델 단독 예측보다 낮은 오차를 보였다. 특히 `LSTM`이 가장 낮은 `MAE`와 `RMSE`를 기록하였다. 그러나 핵심은 특정 모델이 1등이라는 점보다, 세 모델 모두 물리 모델의 예측 오차를 유의미하게 줄였다는 점이다.

![그림 1. 테스트셋에서 물리 모델과 AI 보정 모델의 오차 비교](outputs/default/metrics_comparison.png)

그림 1은 테스트셋에서의 `MAE`와 `RMSE`를 비교한 그래프이다. 물리 모델의 오차 막대가 가장 크게 나타났고, 인공지능 보정 모델을 결합한 경우 오차가 뚜렷하게 감소하였다. 이를 통해 인공지능이 물리 모델의 한계를 보완하는 역할을 했음을 확인할 수 있다.

### 7-2. 시계열 예측 양상 비교

![그림 2. 일부 테스트 태풍 사례에서 실제 강도와 각 모델의 예측 궤적 비교](outputs/default/sample_trajectories.png)

그림 2는 일부 테스트 사례에서 시간에 따른 실제 강도와 각 모델의 예측값을 비교한 것이다. 검은 선은 실제 강도, 회색 점선은 물리 모델의 예측값이다. 파란색, 청록색, 주황색 선은 각각 `MLP`, `LSTM`, `Transformer` 보정 결과를 나타낸다.

그래프를 보면 물리 모델은 전체적인 증가와 감소 추세는 따라가지만, 강화 구간이나 약화 구간의 세부 변화에서 차이가 생기는 경우가 있었다. 반면 인공지능 보정 모델을 결합한 예측은 실제 곡선에 더 가깝게 붙는 경향을 보였다. 이는 인공지능이 물리 모델이 설명하지 못한 비선형 패턴을 추가로 학습했기 때문으로 해석할 수 있다.

### 7-3. 학습 과정 확인

![그림 3. 각 인공지능 보정 모델의 학습 손실과 검증 손실 변화](outputs/default/training_curves.png)

그림 3은 각 모델의 학습 손실과 검증 손실 변화를 나타낸다. 전반적으로 학습이 진행될수록 손실이 감소하였으며, 검증 손실도 함께 낮아지는 경향을 보였다. 이는 모델이 단순히 훈련 데이터에만 맞춘 것이 아니라 검증 데이터에서도 일정 수준 일반화되었음을 보여준다.

## 8. 결과 해석

이번 탐구 결과를 통해 다음과 같은 점을 확인할 수 있었다.

첫째, 단순 물리 모델은 태풍 강도 변화의 전반적인 경향을 설명하는 데에는 충분히 유용하였다. 실제로 물리 모델만으로도 강도가 증가하는 구간과 감소하는 구간의 큰 흐름은 어느 정도 재현할 수 있었다.

둘째, 단순 물리 모델은 세부적인 변화나 복잡한 비선형성을 완전히 설명하지는 못하였다. 특히 급격한 강화 또는 약화가 일어나는 일부 구간에서는 예측 오차가 상대적으로 커졌다.

셋째, 인공지능 보정 모델은 이러한 오차 패턴을 추가로 학습하여 물리 모델의 한계를 보완하였다. 세 인공지능 모델 모두 오차를 줄였다는 사실은, 인공지능이 물리 모델의 대체재라기보다 보완재로 작용할 수 있음을 보여준다.

넷째, 본 실험에서는 `LSTM`이 가장 좋은 성능을 보였다. 이는 최근 시간 흐름에 따른 강도 변화 패턴을 학습하는 데 시계열 모델이 유리했기 때문으로 볼 수 있다. 다만 실험 조건과 데이터 구조가 달라지면 가장 우수한 모델도 달라질 수 있으므로, 여기서의 핵심 결론은 특정 모델의 우열보다 `AI 기반 보정 자체의 효과`에 있다.

## 9. 한계 및 개선점

본 탐구에는 다음과 같은 한계가 있다.

1. 실제 기상청 또는 위성 관측 자료가 아니라 합성 데이터를 사용하였다.
2. 물리 모델이 매우 단순하므로 실제 태풍 물리 과정을 완전히 반영하지는 못한다.
3. 숨겨진 환경 요인을 기준 데이터에는 넣었지만, 보정 모델 입력에는 제한적으로만 반영하였다.
4. 실험 규모가 비교적 작아 보다 다양한 조건에서의 재검증이 필요하다.

향후에는 다음과 같은 방향으로 확장할 수 있다.

1. 실제 태풍 관측 데이터로 실험을 확장한다.
2. 해수면 온도와 위도 외에 이동 속도, 해양 열함량, 연직 바람 전단 등 추가 변수를 포함한다.
3. 물리 모델을 더 정교하게 만든 뒤에도 AI 보정 효과가 유지되는지 확인한다.
4. 예측 구간을 더 길게 늘려 단기 예측과 중기 예측에서의 성능 차이를 분석한다.

## 10. 결론

본 탐구에서는 해수면 온도와 위도 변화에 따른 태풍 강도 변화를 단순 물리 시뮬레이션으로 구현하고, 인공지능 보정 모델을 결합하여 예측 성능 변화를 분석하였다. 그 결과 단순 물리 모델은 태풍 강도 변화의 기본적인 경향을 설명하는 데에는 유용하였으나, 세부적인 비선형 변화에서는 오차가 발생하였다.

반면 인공지능 보정 모델을 적용한 경우 물리 모델의 예측 오차가 뚜렷하게 감소하였다. 특히 테스트셋에서 `MLP`, `LSTM`, `Transformer` 모두 물리 모델보다 더 낮은 `MAE`와 `RMSE`를 보였으며, 가장 좋은 경우 `MAE`가 약 `53.91%` 개선되었다. 따라서 단순 물리 모델과 인공지능 보정 모델을 결합하면 태풍 강도 변화를 더 정확하게 예측할 수 있음을 확인하였다.

즉 본 탐구를 통해 **단순 물리 모델은 태풍 강도 변화의 기본 경향을 설명할 수 있었으며, 인공지능 보정 모델을 결합했을 때 물리 모델의 예측 오차를 줄여 보다 정확하게 태풍 강도 변화를 예측할 수 있음을 확인하였다.**

## 11. 부록: 재현 방법

실험 코드는 다음 명령으로 재실행할 수 있다.

```bash
python -m pip install -r requirements.txt
python main.py --output-dir outputs/default
```

주요 결과 파일은 다음 위치에 저장된다.

- `outputs/default/metrics.csv`
- `outputs/default/metrics_comparison.png`
- `outputs/default/sample_trajectories.png`
- `outputs/default/training_curves.png`
- `outputs/default/summary.json`
